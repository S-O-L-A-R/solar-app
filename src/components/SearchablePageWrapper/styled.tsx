import { Gap } from 'solarxui'
import styled from 'styled-components'

export const Body = styled(Gap)`
	position: relative;
`

export const SearchContainer = styled.div`
	position: sticky;
	top: 20px;
	z-index: 1;
`
