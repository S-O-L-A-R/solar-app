import styled from 'styled-components'
import { gray } from 'solarxui'
import { Gap } from 'solarxui'

export const Time = styled.div`
	color: ${gray[2]};
	font-size: 14px;
`

export const Detail = styled.div`
	font-size: 18px;
`

export const Name = styled(Gap)`
	align-items: center;
`

export const Desc = styled.div`
	font-size: 14px;
`
